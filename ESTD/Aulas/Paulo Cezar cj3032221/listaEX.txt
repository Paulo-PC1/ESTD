1 - Dado o array abaixo, escreva um programa em C++ que calcule e exiba a média, o maior e o menor valor. Use apenas um loop para resolver tudo.

int notas[] = {7, 9, 5, 8, 6, 10, 4};

2 - Escreva uma FUNÇÃO que receba um array e o inverta sem usar um segundo array auxiliar. Em seguida, chame a função no main e imprima o array antes e depois.

3 - Declare duas matrizes 3x3, some-as elemento por elemento armazenando o resultado em uma terceira matriz. Crie uma função separada para imprimir qualquer matriz 3x3.

4 - Escreva uma função recursiva que calcule a soma de todos os números de 1 até N. Demonstre a chamada no main para N = 5 e N = 10.

5 - Escreva uma função recursiva que retorne o N-ésimo número da sequência de Fibonacci. Imprima os 8 primeiros termos da sequência.

6 - Escreva uma função recursiva que inverta uma string sem usar laços (for/while). Teste com as palavras: "radar", "hello" e "estrutura".

7 - Construa uma lista ligada inserindo os valores 10, 20, 30, 40, 50 no final. Imprima a lista no formato abaixo e exiba a quantidade total de nós.

8 - Monte uma lista com os valores 5, 15, 25, 35, 45. Peça ao usuário um valor, busque-o na lista e, se encontrado, remova-o e imprima a lista atualizada. Se não encontrado, exiba mensagem de erro.

9 - Crie uma função que inverta a ordem de uma lista ligada SEM criar uma nova lista. Use três ponteiros (anterior, atual, proximo) para redirecionar os ponteiros um a um.

10 - Implemente uma pilha e realize as seguintes operações em sequência: empilhe 10, 20, 30, 40, 50. Imprima o topo. Desempilhe dois valores. Imprima o novo topo.

11 - Usando uma pilha, escreva uma função que verifique se os parênteses de uma string estão corretamente balanceados. Teste com: "(()())", "((())", "())()" .

Regra: ao encontrar '(' empilhe. Ao encontrar ')' desempilhe — se a pilha estiver vazia, não está balanceado. No final, pilha vazia = balanceado.

12- Implemente uma fila e realize: enfileirar Ana, Bruno, Carla, Diego. Imprima a fila. Desenfileire dois clientes mostrando quem foi atendido. Imprima a fila atualizada.

Saída esperada:
Fila: Ana → Bruno → Carla → Diego → NULL Atendendo: Ana Atendendo: Bruno Fila: Carla → Diego → NULL

13 - Implemente uma fila onde cada elemento possui valor e prioridade (1=alta, 2=média, 3=baixa). O enfileiramento é normal (FIFO), mas o desenfileiramento sempre retira o elemento de MAIOR prioridade (menor número). Dica: Percorra a fila inteira procurando o elemento com menor número de prioridade.

14 - Simule a fila de uma impressora. O usuário pode: (1) adicionar documento, (2) imprimir próximo, (3) ver fila, (0) sair. Cada documento tem nome e número de páginas. Ao imprimir, exiba o documento e total de páginas impressas na sessão.geterson.nunes@ifsp.edu

