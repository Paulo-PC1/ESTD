#include <iostream>
#include <cmath>

int main(void)
{
    /**
      + -> manutencao de sinal
      - -> manutencao de sinal
      % -> resto divisao(modulo)
      / -> divisao
      * -> multiplicaçao
      + -> adicao
      - -> subtracao
      ++ -> incremento
      -- -> decremento
      += -> adicao cumulativa
      -= -> adicao cumulativa
      *= -> subtracao cumulativa
      /= -> divisao cumulativa
      %= -> modulo cumulativa
      pow(b,e) -> potenciacao (cmath)
      cbrt(n) -> raiz cubica (cmath)
      sqrt(n) -> raiz quadrada (cmath)
     */
    
    return 0;
}